//Note: from terminal do    git checkout -- .    if you need to revert project to the most recent commit

import '../styles/styles.css'

if (module.hot) { //This simply says "accept the hot updates from webpack-dev-server if it makes sense to accept them"
    module.hot.accept()
}